# Copy to .env and change as you wish. All values are optional.

# ----- Server -----
PORT=8080
READ_TIMEOUT=5s
WRITE_TIMEOUT=10s
READ_HEADER_TIMEOUT=2s
IDLE_TIMEOUT=60s
SHUTDOWN_TIMEOUT=5s

# ----- Request / QR limits -----
MAX_BODY_SIZE=1048576
MIN_QR_SIZE=64
MAX_QR_SIZE=512
DEFAULT_QR_SIZE=256

# ----- Environment & logging -----
# ENV: development | staging | production (or prod, live). Affects default log format.
ENV=development

# LOG_LEVEL: debug | info | warn | error. Use debug in dev, info/warn in prod.
LOG_LEVEL=info

# LOG_FORMAT: text | json. If unset, text for development and json for production/staging/live.
LOG_FORMAT=
